Label + description on the left, ink switch on the right. Used for settings like hall-pass and notification preferences.

```jsx
<Toggle label="Hall-pass" help="Allow one skip-sick day per cycle." checked={true} />
```
