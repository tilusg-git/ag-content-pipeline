Mono tracked-caps segmented switch — ink fill on the active segment. Used for view toggles (e.g. Week / Month).

```jsx
<SegmentedControl options={[{value:"week",label:"Week"},{value:"month",label:"Month"}]} defaultValue="week" />
```
