Circular custom radio — ember dot when selected. Group with a shared `name`.

```jsx
<Radio name="policy" label="Redistribute to top finishers" />
<Radio name="policy" label="Donate to a shared charity" />
```
