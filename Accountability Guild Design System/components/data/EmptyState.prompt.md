Dashed-border panel for zero-data states — large italic-serif glyph, mono kicker, italic h3, editorial body.

```jsx
<EmptyState glyph="0" kicker="No cycles yet" title="Nothing to report yet" actions={<Button arrow>Start a cycle</Button>}>
  Set the standard once, then run your first cycle.
</EmptyState>
```
