A bordered `<List>` of `<ListRow>`s — serif title, sans meta line, hover fill. Used for member lists, goal lists, activity feeds.

```jsx
<List>
  <ListRow num="01" title="Lift three times" meta="3 of 3 · this week" end={<Badge tone="success" dot>Kept</Badge>} />
</List>
```
