Multi-line text field for cycle reports, commitments, and notes — pairs with a right-aligned mono character counter.

```jsx
<TextareaField label="Cycle report" counter="120 / 280" maxLength={280} placeholder="What happened this cycle?" />
```
