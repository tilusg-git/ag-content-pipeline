Centered dialog on a dark scrim — ink border, no radius, serif title with optional italic emphasis.

```jsx
<Modal open kicker="Leave group" title="Leave Legacy Group?" destructive
  actions={<><Button variant="ghost">Cancel</Button><Button variant="destructive">Leave</Button></>}>
  You'll forfeit this cycle's stake and lose access to the group feed.
</Modal>
```
