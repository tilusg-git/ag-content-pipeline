# Site repo lives here as its own separate git checkout — never tracked by this repo
repo/

# Node deps in the card-generator assets
node_modules/
**/node_modules/

# Credentials — never tracked, never pushed (this repo is public)
secrets/

# Local tooling state — never pushed
.claude/

# OS / editor cruft
.DS_Store
Thumbs.db
